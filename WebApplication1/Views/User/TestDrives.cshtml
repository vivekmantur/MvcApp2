@model IEnumerable<WebApplication1.Models.TestDrive>

@{
    ViewData["Title"] = "Your TestDrives";
}

<div class="container mt-5">
    <div class="row mb-4">
        <div class="col text-center">
            <h2>@ViewData["Title"]</h2>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <table class="table table-striped table-bordered">
                        <thead class="thead-dark">
                            <tr>
                                <th>TestDrive Date</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var request in Model)
                            {
                                <tr>
                                    <td>@request.Testdrivedate</td>
                                    <td>
                                        @if (request.Status==TestdriveStatus.Completed)
                                        {
                                            <div class="status-card bg-success text-white p-2 rounded">
                                                <i class="fas fa-check-circle"></i> Completed
                                            </div>
                                        }
                                        else if (request.Status == TestdriveStatus.Pending)
                                        {
                                            <div class="status-card bg-warning text-dark p-2 rounded">
                                                <i class="fas fa-hourglass-half"></i> Pending
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="status-card bg-danger text-white p-2 rounded">
                                                <i class="fas fa-times-circle"></i> Unsuccess(Car is sold)
                                            </div>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@* Add this CSS to improve styling *@
<style>
    .status-card {
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-size: 14px;
    }
    .status-card i {
        margin-right: 8px;
    }
</style>
